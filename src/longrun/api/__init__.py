"""FastAPI over the job runner, backing the web UI (scope 10.3).

Same jobs the CLI and MCP server use; the plan schema in core.models is the API contract.
A client, never the only way to do something.
"""
